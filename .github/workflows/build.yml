name: Build Android APK

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    # Use Ubuntu 22.04 instead of 'ubuntu-latest' (24.04) to avoid toolchain breakages
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python 3.10
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Install System Dependencies for Buildozer
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential \
            git \
            ccache \
            libffi-dev \
            libssl-dev \
            libsqlite3-dev \
            zlib1g-dev \
            openjdk-17-jdk \
            autoconf \
            libtool \
            pkg-config \
            cmake \
            zip \
            unzip \
            curl

      - name: Install Buildozer & Cython
        run: |
          python -m pip install --upgrade pip setuptools
          pip install buildozer "cython<3.0.0"

      - name: Pre-download recipe cache (bypass Savannah server 502 errors)
        run: |
          mkdir -p ~/.buildozer/cache/freetype
          curl -sSL "https://ftpmirror.gnu.org/freetype/freetype-2.14.1.tar.gz" -o ~/.buildozer/cache/freetype-2.14.1.tar.gz
          cp ~/.buildozer/cache/freetype-2.14.1.tar.gz ~/.buildozer/cache/freetype/

      - name: Build APK with Buildozer
        run: |
          yes | buildozer -v android debug

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: MangaTranslator-APK
          path: bin/*.apk
